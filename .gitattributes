# Handle line endings automatically for files detected as text
# and leave all files detected as binary untouched.
* text=auto

# Force the following file types to have unix line endings (LF)
*.css text eol=lf
*.html text eol=lf
*.js text eol=lf
*.json text eol=lf
*.md text eol=lf
*.sh text eol=lf
*.txt text eol=lf
*.yml text eol=lf

# Force the following file types to have windows line endings (CRLF)
*.bat text eol=crlf

# Specify that binary files shouldn't be messed with
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary

# Export-ignore files that shouldn't be part of the final distribution
.gitattributes export-ignore
.gitignore export-ignore
README.md export-ignore
CONTRIBUTING.md export-ignore
LICENSE export-ignore

# Define custom diff drivers (uncomment when needed)
# *.extension diff=custom_diff_tool
